# Write a simple guessing game, where the user has to guess a number between 1-100.
# The program should give a hint for each guess, if the number is lower or higher.

import random
guess_this_number = random.randint(1,100)